pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -l -w {staged_files}

    lint:
      glob: "*.go"
      run: golangci-lint run --enable=gosec ./...

pre-push:
  commands:
    test:
      run: go test -v -race ./...
